# Gemini AI Prompt

Create a Python script that opens a popup window reminding the user to stand up and stretch every 30 minutes. The popup should display a motivational message and a random stretch tip. After the user closes the popup, the 30-minute timer should reset and repeat automatically. Use only Python's built-in libraries (tkinter and time).
